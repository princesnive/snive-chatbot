Create Table compaines {
    id int,
    name varchar(255),
    address varchar(255),
    phone varchar(255),
    email varchar(255),
    website varchar(255),
    created_at timestamp,
    updated_at timestamp,
    deleted_at timestamp,
    primary key (id)
}